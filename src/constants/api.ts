export enum TimeZone {
  UTC = "UTC",
  SEOUL = "Asia/Seoul",
}
export enum DateFormat {
  YYYY_MM_DD_HH_II_SS = "%Y-%m-%d %H:%i:%s",
}

/**
 * 무기 타입
 */
export enum WeaponType {
  /**
   * 근접
   */
  MELEE = "MELEE",
  /**
   * 원거리
   */
  RANGED = "RANGED",
}
